const grid=document.getElementById('grid');const files=document.getElementById('files');const status=document.getElementById('status');const message=document.getElementById('message');const boot=document.getElementById('boot');let found=0;for(let i=1;i<=7;i++){const c=document.createElement('button');c.className='cell';c.textContent='??';c.setAttribute('aria-label','Archive fragment '+i);c.onclick=()=>{if(c.classList.contains('found'))return;c.classList.add('found');c.textContent='✓';found++;files.textContent=found;if(found===7){status.textContent='RECOVERED';message.textContent='BUILD 07 RECOVERED. The missing game can finally boot.'}else message.textContent=`Fragment ${i} recovered. ${7-found} remain.`};grid.appendChild(c)}boot.onclick=()=>{if(found===7){message.textContent='BOOT OK — WELCOME TO THE LOST 2007 BUILD. 🎮'}else{message.textContent='BOOT FAILED — recover all 7 fragments first.'}};